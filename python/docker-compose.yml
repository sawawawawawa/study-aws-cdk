version: '4'

services:
  app:
    image:
    container_name: study-aws-cdk-python
    build:
      context: .
    volumes:
      - .:/usr/local/study-aws-cdk
    working_dir: /usr/local/study-aws-cdk
    tty: true